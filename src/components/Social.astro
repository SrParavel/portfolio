---
const { site, username } = Astro.props;
const address = `https://www.${site}.com/${username}`;
---

<a
    class="bg-gray-300 rounded-md px-3 py-2 text-sm font-bold hover:shadow-sm hover:scale-105 duration-300 ease-in-out"
    href={address}>{site}</a
>
