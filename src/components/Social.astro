---
const { site, username } = Astro.props;
const address = `https://www.${site}.com/${username}`;
---

<a
  class="bg-gray-100 rounded-full px-4 py-2 text-sm font-light hover:shadow-sm hover:scale-105 duration-300 ease-in-out"
  target="_blank"
  href={address}>{site}</a
>
