---
const { site, username } = Astro.props;
const address = `https://www.${site}.com/${username}`;
---

<a
  class="rounded-full px-4 py-2 text-sm font-normal hover:shadow-sm hover:scale-105 duration-300 ease-in-out border-[1px]"
  target="_blank"
  href={address}>{site}</a
>
